<li>
	{{burger_name}}

	<button class="change-devour" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devour}}DEVOUR!{{else}}WAKEUP!{{/if}}
	</button>


{{!-- ADD DELETE BUTTON -- HIDE DATA ID UNDER DATA ATTRIBUTE WITH HANDLEBAR ID --}}
	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
